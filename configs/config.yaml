# configs/config.yaml
data:
  path: "data"
  subject: "Abram"
  demographic: "novice"

model:
  k: 15
  dropout: 0.7

training:
  epochs: 3
  batch_size: 1
  learning_rate: 0.0001
  checkpoint_dir: "checkpoints/"

evaluation:
  metrics:
    - "mse"