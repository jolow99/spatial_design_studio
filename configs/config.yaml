# configs/config.yaml
data:
  path: "data"
  subject: "subject1"
  demographic: "novice"

model:
  k: 15
  dropout: 0.6

training:
  epochs: 1
  batch_size: 1
  learning_rate: 0.0001
  checkpoint_dir: "checkpoints/"

evaluation:
  metrics:
    - "mse"